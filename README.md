# FTP connector via implicit FPTS

This is a sample for connect to FTPS as implicit FPTS with support session tickets.  
Many FTP servers make reconnection for simple action like start retrieving file.